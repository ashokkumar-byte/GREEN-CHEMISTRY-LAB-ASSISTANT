async function api(url,options={}){const r=await fetch('/api'+url,{credentials:'same-origin',headers:{'Content-Type':'application/json',...(options.headers||{})},...options});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Request failed');return d}
function show(id,msg,ok=false){const e=document.getElementById(id);if(e){e.textContent=msg;e.className=ok?'success':'error'}}
const loginForm=document.getElementById('loginForm');if(loginForm)loginForm.onsubmit=async e=>{e.preventDefault();try{await api('/login',{method:'POST',body:JSON.stringify({username:loginForm.username.value,password:loginForm.password.value})});location.href='dashboard.html'}catch(x){show('msg',x.message)}};
const reg=document.getElementById('registerForm');if(reg)reg.onsubmit=async e=>{e.preventDefault();if(reg.password.value!==reg.confirm_password.value)return show('msg','Passwords do not match');try{await api('/register',{method:'POST',body:JSON.stringify({name:reg.name.value,username:reg.username.value,email:reg.email.value,password:reg.password.value})});show('msg','Account created. You can login now.',true);reg.reset()}catch(x){show('msg',x.message)}};
async function logout(){await api('/logout',{method:'POST'});location.href='login.html'}
